# Frontegg Configuration (Required)
# Get these from: Frontegg Portal → [Environment] → Settings → API Keys
FRONTEGG_CLIENT_ID=your-client-id-here
FRONTEGG_API_KEY=your-api-key-here
FRONTEGG_BASE_URL=https://app-xxx.frontegg.com
FRONTEGG_REGION=eu  # Options: eu, us, au, ca

# Frontegg API Token for Entitlements Agent (Required for Docker)
# Get these from: Frontegg Portal → Administration → Personal Tokens
FRONTEGG_API_TOKEN_CLIENT_ID=your-personal-token-client-id
FRONTEGG_API_TOKEN_SECRET=your-personal-token-secret

# Server Configuration
PORT=5000
NODE_ENV=development

# Services
ENTITLEMENTS_AGENT_URL=http://localhost:8181
FRONTEND_URL=http://localhost:3000

# Optional: Disable ReBAC for testing without Docker
# USE_MOCK_REBAC=true

# Note: SQLite database will be created automatically as database.sqlite in the backend folder